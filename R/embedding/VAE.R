#' create single cell matrix embedding via VAE
#' 
#' @param x a gcmodeller HTS matrix object
#' @param dims the embedding dimension size
#' 
const vae_embedding = function(x, dims = 9, batch_size = 100, iterations = 1000) {

}